import PropTypes from 'prop-types';

export const propTypes = {
  addressRequest: PropTypes.object,
  tokenizeActions: PropTypes.object,
  assets: PropTypes.array,
  accountPlainId: PropTypes.string
};
