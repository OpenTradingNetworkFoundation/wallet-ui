import PropTypes from 'prop-types';

export const propTypes = {
  availableAssets: PropTypes.array,
  selectedAsset: PropTypes.object,
  excludedAsset: PropTypes.object,
  onChange: PropTypes.func
};
