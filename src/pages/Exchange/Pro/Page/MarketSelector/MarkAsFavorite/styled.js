import styled from 'styled-components';

export const Mark = styled.button`
  background: none;
  border: none;
  outline: none;
  cursor: pointer;

  & > svg {
    width: 13px;
    height: 14px;
  }
`;
